{% extends 'base1.html.twig' %}

{% block title %}Search{% endblock %}

{% block body %}
    <h1>Search</h1>
    
    {{ form_start(form) }}
        {{ form_row(form.email) }}
		
        {{ form_row(form.creator) }}
		{{ form_row(form.creator.Nom) }}
        <button type="submit">Search</button>
    {{ form_end(form) }}
{% endblock %}