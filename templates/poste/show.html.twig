{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}

{% block contentblock %}
   

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ poste.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ poste.description }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ poste.img }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ poste.domaine }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ poste.idUser.getnom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ poste.date ? poste.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_poste_index') }}"><button>back to list</button></a>

    <a href="{{ path('app_poste_edit', {'id': poste.id}) }}"><button>edit</button></a>

    {{ include('poste/_delete_form.html.twig') }}
   

    


    <h1>Liste des Commentaires</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                <td>{{ commentaire.id }}</td>
                <td>{{ commentaire.description }}</td>
                <td>{{ commentaire.date ? commentaire.date|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_commentaire_show', {'id': commentaire.id}) }}"><button>show</button></a>
                    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}"><button>edit</button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_new') }}">Create new</a>


{% endblock %}
