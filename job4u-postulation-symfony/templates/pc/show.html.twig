{% extends 'base.html.twig' %}

{% block title %}Postulation{% endblock %}

{% block body %}
    <h1>Postulation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPos</th>
                <td>{{ postulation.idPos }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ postulation.email }}</td>
            </tr>
            <tr>
                <th>Date_pos</th>
                <td>{{ postulation.datePos ? postulation.datePos|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pc_index') }}">back to list</a>

    <a href="{{ path('app_pc_edit', {'idPos': postulation.idPos}) }}">edit</a>

    {{ include('pc/_delete_form.html.twig') }}
{% endblock %}
